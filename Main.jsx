import React from 'react'
import ReactDOM from 'react-dom/client'
import CustomsSimulation from './CustomsSimulation'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CustomsSimulation />
  </React.StrictMode>,
)
